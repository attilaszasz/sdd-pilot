---
agent: Software Engineer
---
Load and follow the workflow in `.github/skills/autopilot-pipeline/SKILL.md`.

Set `AUTOPILOT = true`. Never prompt the user. When the workflow says **Delegate**, resolve the referenced sub-agent at that point and execute the delegated work for real.
